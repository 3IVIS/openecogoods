// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// Project site on GitHub Pages: https://3ivis.github.io/openecogoods/
// Update `site`/`base` (or remove `base`) once a custom domain is set up.
export default defineConfig({
  site: 'https://3ivis.github.io',
  base: '/openecogoods',
});
